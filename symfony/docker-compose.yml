version: '2'

services:
    php:
        build:
            context: ./php
        volumes:
            - ./app:/symfony-project
    nginx:
        build: nginx
        ports:
            - 8000:80
        volumes_from:
            - php
